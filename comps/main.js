import React from 'react';
import {View, Text} from 'react-native';
import  Right from './right';

function Main(){
    return(
        <View style={{flex:1}}>
            <Right />
        </View>
    );
}

export default Main;
